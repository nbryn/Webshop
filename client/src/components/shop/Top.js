import React, { Component } from "react";

const Top = props => {
  return (
    <div className="top_shop">
      <div className="container" />
      {props.title}
    </div>
  );
};

export default Top;
